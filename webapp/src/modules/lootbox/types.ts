import { Address } from 'web3x-es/address'

export enum LootboxType {
  Basic,
  Medium,
  Premium
}

export type Lootbox = {

}

export enum TransactionType {
  Approve = 'approve',
  Transfer = 'transfer'
}
